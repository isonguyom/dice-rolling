# Request the number(1-4) of six-sided dice to be rolled from the user
num_of_dice = int(input("Please, how many dice do you want to roll (1–4)? "))
# Accept and parse the user input
# Roll the dice
# Print the generated number